{
  "name": "agent-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Better Auth Agent Auth protocol and execution support",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@better-auth/agent-auth": "0.6.2",
    "better-auth": "^1.6.19"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "typescript": "^5",
    "vitest": "^5.0.0"
  }
}

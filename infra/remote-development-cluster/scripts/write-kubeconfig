#!/usr/bin/env bash
set -euo pipefail

destination="${1:-$PWD/.kubeconfig}"
umask 077

mkdir -p "$(dirname "$destination")"
bash scripts/doppler-terraform-env terraform output -raw kubeconfig > "$destination"
echo "Wrote candidate kubeconfig to $destination"
